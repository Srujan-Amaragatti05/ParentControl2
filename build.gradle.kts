buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.0") // Adjust this if needed
        classpath("com.google.gms:google-services:4.3.10") // Firebase plugin version
    }
}
